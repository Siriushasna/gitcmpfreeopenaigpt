# gitcmpfreeopenaigpt

GitMCP Converter — simple webapp statique (HTML/CSS/JS) pour convertir des URLs GitHub en URLs GitMCP, avec historique par dossier, export/import JSON, PWA et service worker.

Déposer ces fichiers à la racine du repo puis activer GitHub Pages (branch main, dossier /root) pour publier.
